services:
  - type: web
    name: agrovet-api
    env: python
    buildCommand: ./render-build.sh
    startCommand: uvicorn backend.main:app --host 0.0.0.0 --port 10000
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.0
